apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: onlineboutique
  namespace: flux-system
spec:
  interval: 10m
  targetNamespace: onlineboutique
  chart:
    spec:
      chart: podinfo
      sourceRef:
        kind: HelmRepository
        name: onlineboutique
